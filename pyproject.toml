[project]
name = "auto-example-env"
version = "0.1.0"
description = "Generate example.env from .env file for documentation purposes"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
auto-example-env = "main:main"

[tool.setuptools]
py-modules = ["main"]
